# 产品与技术设计

## 2026-08-05 页面基准线、安全区域与旋转适配

- 常规页面统一使用 `AppTheme.HEADER_HEIGHT`、`AppTheme.PAGE_PADDING` 和固定返回按钮槽位，首页、模板、历史、计数器与自定义颜色页从同一安全区下边界开始绘制正文。
- 底部导航入口使用固定图标槽位和文字槽位，图标大于文字并位于文字正上方；选中状态只改变颜色和字重，不改变布局尺寸。
- 页面背景、首页底部导航与系统导航安全区统一使用 `#030A16`，避免页面底部出现不同深色带。
- 常规页面窗口开启全屏布局以绘制安全区背景，根容器使用统一的上下安全区留白和 `expandSafeArea`，保证背景覆盖系统区域而标题、按钮和正文不进入安全区。
- 常规页面窗口方向恢复为 `window.Orientation.UNSPECIFIED`，跟随设备传感器；`DisplayPage` 仍按 `BannerConfig.isLandscape` 使用显式横屏或竖屏全屏。
- 首页预览、输入、模式、颜色、滑杆和其他设置使用更宽的区块间距，模板卡片、历史列表和自定义颜色页同步增加内容间距。

## 产品目标

将手机屏幕变为LED滚动显示屏，覆盖应援、接机、表白、促销、通知、互动6大场景。零门槛即开即用，一个月达400月活。

## 用户与场景

| 目标用户 | 核心场景 | 成功指标 |
|---------|---------|--------|
| 追星粉丝 | 演唱会应援 | 3秒出效果 |
| 年轻人 | 表白/接机 | 模板一键用 |
| 小商户 | 促销广告 | 每日复用 |

## 页面信息架构

| 页面 | 职责 | 入口 | 主要状态 |
|------|------|------|--------|
| Index | 配置LED参数 | 启动首页 | 输入中/预览中 |
| DisplayPage | 全屏LED显示 | 首页开始按钮 | 播放中/暂停 |
| TemplatePage | 场景模板选择 | 底部导航 | 分类筛选 |
| HistoryPage | 历史记录管理 | 底部导航 | 空/有数据 |
| ColorPickerPage | 自定义颜色 | 配色区跳转 | HSV/RGB调节 |
| CounterPage | 计数器 | 底部导航 | 全屏计数 |

## 核心交互

- 首页配置实时预览
- 全屏显示双击暂停/继续
- 单击显示退出按钮（3秒自动隐藏）
- 模板点击直接进入全屏显示
- 历史记录点击恢复配置并显示

## 视觉系统

- 风格：暗色系为主（#1A1A1A背景），LED荧光绿(#00FF00)为品牌色
- 排版：标题20fp，正文14fp，辅助12fp
- 圆角：卡片12vp，按钮26vp，芯片16vp
- 配色：11套预设+1个自定义HSV/RGB入口

## 多设备适配

| 断点 | 范围 | 策略 |
|------|------|------|
| SM | <600vp | 单列布局 |
| MD | 600-840vp | 双列配置 |
| LG | >840vp | 左右分栏 |

## 状态与数据

- 核心状态：BannerConfig（当前配置）
- 持久化：Preferences存储（历史记录、上次配置、自定义颜色）
- 无网络、无登录、无云同步

## 无障碍

- 对比度：暗底亮字，天然高对比
- 触控目标：所有按钮>=48vp
- 语义标签：关键操作按钮有明确文字

## 技术权衡

| 决策 | 选择 | 原因 |
|------|------|------|
| 动画方案 | animateTo | ArkUI原生，性能最优 |
| 存储方案 | Preferences | 轻量无权限，适合配置数据 |
| 颜色选择器 | HSV滑块+RGB输入 | 比Canvas色轮实现简单可靠 |
| 霓虹效果 | 颜色数组轮换 | 避免复杂shader |

## 2026-08-03 视觉重设计

### 视觉语言

- 页面统一使用 `#030A16` 深黑蓝背景、`#00E5FF` 青色主强调和 `#168BFF` 蓝色辅助强调。
- 面板采用深蓝表面、细描边与克制的青色发光；LED 内容使用可复用点阵底纹和等宽高亮文字。
- 主按钮使用青色到亮蓝渐变，危险与成功操作分别使用红色和绿色。

### 组件与布局

- `common/Theme.ets` 集中维护颜色、圆角和页面间距。
- `components/LedPanel.ets` 统一首页、模板、历史和颜色页的点阵预览。
- 首页主操作固定在底部导航上方，内容区保持可滚动，所有主要设置在标准 phone 首屏可见。
- 配置页宽屏最大内容宽度为 620vp，历史页为 720vp；模板页在 600vp/840vp 断点切换 2/3/4 列。
- 计数器面板和操作区限制最大宽度，避免 tablet/2in1 上过度拉伸。

## 2026-08-04 横屏展示与沉浸式安全区

> 历史说明：本节记录 2026-08-04 当时的窗口方案；其中常规页非全屏、状态栏不透明以及退出后固定恢复竖屏等表述，已由下方 2026-08-07 的全屏透明状态栏、安全区和窗口恢复规则取代，不再作为当前实现指导。

### 窗口状态

- 常规页面统一使用系统安全区：关闭布局全屏、显示状态栏和导航栏，页面内容不绘制到两个系统栏区域。
- 状态栏使用 `#030A16`，导航栏使用 `#01050D`，图标使用浅色，和深黑蓝页面主题连续一致。
- LED 展示页根据 `BannerConfig.isLandscape` 设置首选方向；开启时请求横屏，关闭时请求竖屏。两种模式都会进入全屏展示、隐藏系统栏、保持常亮并使用最高亮度。
- 离开展示页始终恢复竖屏、标准安全区、系统栏配色、自动亮度和正常息屏策略，避免页面间遗留窗口状态。

### 实现边界

- `ScreenService` 作为窗口状态唯一入口，页面不再各自组合全屏、方向、亮度和常亮 API。
- `DisplayPage` 在窗口状态切换后读取当前显示尺寸，用于横向滚动和弹幕的距离计算；`CounterPage` 保持常规安全区布局。
- DevEco Emulator 当前仅报告 `1320x2856` 一种竖屏物理显示模式，因此无法在该模拟器中确认实际横屏画布；真机或支持旋转的模拟器应作为横屏方向的最终验收设备。

## 2026-08-05 可靠性与错误态补充

- `StorageService` 将 Preferences 初始化合并为单一异步过程，并串行化写入；配置、历史和自定义颜色读取会过滤损坏或不完整的数据。
- 首页、模板、历史、颜色和计数器页面在异步操作期间保留可重试状态，并对失败显示明确反馈；重复点击不会创建并发保存或重复跳转。
- 展示页在页面隐藏时暂停动画，在页面恢复时重新启动；所有定时器（包括首次引导、弹幕延迟和弹幕移动）在页面离开时清理。
- 弹幕位置通过数组替换触发 ArkUI 刷新；模板图标字段使用语义化键，模板卡片继续以真实字幕预览为主。
- 本轮不改变页面信息架构、主题色、路由、联网边界或设备目标；横屏实际画布仍需支持旋转的独占设备完成最终验收。

## 2026-08-05 底部导航标签对齐

- 底部导航的四个栏位继续使用固定的 25% 宽度，图标保持位于文字上方的固定槽位。
- 每个标签使用 `TextAlign.Center`，确保“首页、模板、历史、计数器”分别居中对齐到对应图标，不改变图标、路由或点击区域。

## 2026-08-05 首页其他设置垂直节奏

- “其他设置”面板的三行开关使用统一的 `SETTING_ROW_HEIGHT`，让图标、文字和开关在更宽松的行高内垂直居中。
- 面板增加统一的上下内边距，扩大行与面板边界之间的呼吸空间；内容仍位于首页滚动区域内，不改变开关交互。

## 2026-08-05 常规 UI 字号微调

- 在 `AppTheme` 中增加统一的 `uiFontSize()`，对常规操作页面的固定 UI 文字使用 1.08 倍字号缩放。
- 首页、模板、历史、自定义颜色、计数器和展示页提示层同步使用该缩放规则，保持跨页面的文字层级一致。
- 用户在首页设置的 LED 字幕字号，以及计数器的大数字展示保持原有动态尺寸，避免改变核心内容的视觉比例。

## 2026-08-05 常规 UI 字重微调

- 在 `AppTheme` 中增加统一的 `UI_FONT_WEIGHT`，常规正文、标签、数值和操作提示使用 `FontWeight.Medium`。
- 页面标题、主操作按钮和 LED 字幕继续使用已有的 Bold 层级，图标符号保持各自的视觉重量。

## 2026-08-05 应用图标资源规范

- 本节记录 2026-08-05 当时的单图标方案，属于历史说明；应用与 Ability 图标的当前规则已由下方 2026-08-07 分层图标设计约束取代。
- 应用级图标和 Ability/启动窗口图标均使用实际 LED 设备图形，不再使用纯色占位资源。
- 主图标资源保持 1024x1024 分辨率，并保留主体周围的安全留白，以适配不同设备的图标裁切和缩放。
- `AppScope/app.json5` 继续引用 `$media:app_icon`；`entry/src/main/module.json5` 的 Ability 图标和启动窗口图标继续引用 `$media:startIcon`。
- 图标资源检查作为标准回归的一部分，防止低分辨率或纯色占位图再次进入发布包。

## 2026-08-07 审核问题修复设计约束

- 固定 UI 使用 `TEXT_MUTED #8793A8`、`ON_ACCENT #00131A`。实算对比度为主文字/页面 18.66:1、次文字/抬升表面 7.91:1、弱化文字/抬升表面 5.56:1、强调色上文字/青色 12.32:1、蓝色 5.57:1、危险色 5.83:1、成功色 10.13:1；用户生成的 LED 前景/背景色仍可配置，不强制改写。
- 常规页面以全屏布局把 `PAGE_BG` 延伸到安全区，状态栏透明且使用浅色图标/内容，导航栏为 `PAGE_BG`；共享内边距避开系统信息。`DisplayPage` 隐藏两类系统栏，退出路径统一恢复应用 chrome。
- 精确五个真实滚动容器使用 `EdgeEffect.Spring` 且 `alwaysEnabled: true`：首页主 `Scroll`、颜色页 `Scroll`、模板分类横向 `Scroll`、模板内容纵向 `Grid`、历史 `List`。首页“显示模式”和“颜色方案”两个固定布局 `Grid` 明确排除。
- 应用层资源为 `app_icon.json`、`app_icon_background.png`、`app_icon_foreground.png`，Ability 层为 `layered_image.json`、`icon_background.png`、`icon_foreground.png`，启动图为 `startIcon.png`；PNG 均为 1024x1024。背景完全不透明且四角覆盖、不预裁圆角，前景透明且所有 alpha 内容位于四边至少 80px 的安全区内。
- 清单引用分别为 app `$media:app_icon`、Ability `$media:layered_image`、splash `$media:startIcon`。背景层 SHA-256 均为 `8F045ED494D2BC05E802FCC8F56FCE5C9BE46FB7B01E55CD99A59C8EC465DBFD`，前景层均为 `2F7D8E0E60C681D779F925705291892CC0AA39CCD7C3463EBBA16ADA09067515`；`generate-layered-icon.ps1` 确定性生成，`test-icon-assets.ps1` 校验尺寸、透明度、80px 安全边界、引用和哈希一致性。
- 展示页固定覆盖层使用 `DISPLAY_OVERLAY_FG #FFFFFF` 和 `DISPLAY_OVERLAY_BG #07111F`，对比度为 18.94:1。暂停标签、首次引导和退出按钮始终使用这组固定颜色，不继承用户配置的 LED 前景色或背景色；因此即使选择“反转白”等白色展示背景，操作 UI 仍保持独立、可读。

## 2026-08-07 纵向字幕完整进出边界

- `DisplayPage` 的主字幕位于全屏容器中心，纵向动画通过 `translate(y)` 移动，因此屏外边界必须按中心坐标计算。
- 文字单行高度估算为 `fontSize * 1.2`，上下对称移动极限为 `(screenHeight + textHeight) / 2`。由下向上时从正极限开始、到负极限结束；由上向下时反向使用。
- 该几何保证文字的近侧边缘从屏幕外开始，远侧边缘完全越过另一侧后才重置。总路程仍为 `screenHeight + textHeight`，不改变现有速度档位、循环时长、暂停恢复或其他显示模式。

## 2026-08-07 纵向字幕平滑入场

- 纵向循环在首次起步和每次回到屏外起点时，把 `textOpacity` 设为 `0.0`，避免文字的首批可见像素以满亮度直接闪现。
- 每帧以 `abs(offsetY - startPos)` 计算已移动距离，并在一个估算文字高度 `max(textHeight, 1)` 内把透明度线性提升到 `1.0`；超过该距离后保持完全不透明。
- 该规则同时适用于由下向上和由上向下，仅优化入场观感。对称出屏边界、速度档位、总路程、暂停恢复、镜像和其他显示模式不变。
- 页面方向继续由用户现有“横屏显示”开关决定，不新增竖屏锁定；“纵向”只描述当前画布从下边到上边或反向的移动轴。

## 2026-08-07 API 22 上架就绪修复

- 保持现有签名、bundleName、versionCode 和 SDK 编译链不变；发布包的 compatible/target API 均为 22。
- 展示页用原生排他手势组识别单双击，双击优先于单击，避免计时器计数造成暂停和退出按钮互相干扰。
- 历史项打开前先保存配置、增加使用次数并重新读取记录；列表键由稳定 ID、使用次数和最后使用时间共同组成，使 ArkUI 在可变字段更新时重建对应卡片。
- 应用继续使用本地 Preferences，无请求权限、网络 SDK 或请求代码；备份恢复关闭，避免本地字幕和历史记录离开设备。
- 全局 ArkUI 弃用入口迁移到页面 `UIContext`，页面路由、提示、上下文、像素换算和动画均由当前 UI 实例获取。

## 2026-08-11 AppGallery 验收修复设计约束

- AppScope `app_name` 与 EntryAbility 的 label/description 统一为 `光迹字幕`；bundleName、版本、签名、路由和权限保持不变。
- 普通页面使用非全屏窗口布局，系统保留状态栏和导航栏安全区；状态栏与页面背景均为 `PAGE_BG #030A16`，并继续使用浅色系统图标。五个普通页面不再扩展到系统安全区，现有页面内边距与布局结构保留。
- `DisplayPage` 仍是独立的全屏展示模式：进入时全屏并隐藏系统栏，退出时通过 `applyAppChrome()` 恢复普通页窗口状态。
- 固定 UI 文本的最低基准字号为 `uiFontSize(12)`；用户可配置的 LED 字幕字号不受此规则限制。固定颜色对比度继续由 accessibility 合同校验。
- AppScope 与 Ability 层的背景和前景图层保持像素一致，分别通过两个分层描述符引用；验收合同同时锁定名称、窗口策略、安全区、固定字号和展示页全屏行为。

## 2026-08-11 名称与 AppGallery 复核约束

- `光迹字幕` 是唯一的用户可见身份：AppScope 启动器标签、EntryAbility label/description、Index 标题和 AppGallery Connect 商品名称均使用这个精确名称。bundle、版本、路由、签名配置、权限和 AGC 元数据保持不变。
- 应用和 EntryAbility 图标继续通过独立分层描述符引用像素完全一致的 1024x1024 前景/背景 PNG 对。启动器和最近任务最终形状由系统处理，资源源图保持一致。
- 普通页面采用非全屏系统栏布局，`PAGE_BG #030A16` 覆盖可读的浅色系统内容。仅 `DisplayPage` 进入全屏并隐藏系统栏，退出后恢复普通页面 chrome。
- 固定文本执行既有 12fp 验收基线；固定正文对比度至少 4.5:1，标题和图标内容至少 3:1。用户可配置的 LED 内容不是固定应用 chrome。

## 2026-08-11 排查表全量审计补充约束

- 普通页底部系统安全距离统一使用 `SYSTEM_BOTTOM_PADDING = 28`；应用内容仍由非全屏窗口先避开系统导航区域，再保留 28vp 的页面内间距。首页开始按钮和底部 tab 不得进入该间距或系统手势区。
- 首页三个设置开关不再依赖系统浅色/深色主题的原生默认轨道颜色。选中轨道使用 `ACCENT #00E5FF`，关闭轨道使用 `TOGGLE_OFF_TRACK #8793A8`，滑块使用 `TOGGLE_THUMB #00131A`，设置面板统一为 `SETTING_PANEL_BG #091426`。
- 开关关闭轨道对设置面板为 5.94:1，滑块对关闭轨道为 6.11:1，滑块对选中轨道为 12.32:1；开启/关闭同时由滑块位置和颜色表达，不以颜色作为唯一状态反馈。
- 审核表中提醒、代理提醒、账单、统计、默认列表/网格、日历节假日、专注、时钟和白噪音均不属于本产品边界；这些条目必须记录为不适用，不能通过新增无关功能改变产品范围。
- 应用名称、图标、安全区、固定字号、响应式宽度和固定对比度继续沿用现有验收合同；AppGallery Connect 名称/图标、软件资质和 Release 签名属于外部提交交接，不把签名材料写入仓库。
## 2026-08-11 底部系统安全区主题

- 普通页面继续使用非全屏窗口并保留系统安全区，但窗口背景色明确设为 `AppTheme.PAGE_BG`，与页面根背景和底部导航保持同色。
- `EntryAbility` 在 `loadContent` 成功后重新调用 `ScreenService.applyAppChrome()`，确保系统加载过程不会把手势安全区恢复成默认白色。
- 展示页仍由 `enterDisplayMode()` 独立控制全屏和隐藏系统栏，不改变 LED 展示交互。

## 2026-08-11 排查表复核设计约束

- 应用身份保持单一来源：AppScope、EntryAbility 和首页标题都使用 `光迹字幕`；应用级和 Ability 级分层图标继续共享像素一致的背景/前景 PNG，确保启动器与最近任务由系统生成时仍来自同一视觉源。
- 首页预览和计数器面板的点阵背景虽然是装饰层，仍以 `Text` 控件渲染，因此统一使用 `LED_MATRIX_DOT_FONT_SIZE = 10`、`LED_MATRIX_DOT_COLOR = #8793A8`、`LED_MATRIX_DOT_OPACITY = 0.85`。它们不再继承用户 LED 颜色或低透明度蓝色，避免用户选择深色字幕时点阵文字失去可读性。
- 普通页继续使用非全屏窗口和 `SYSTEM_TOP_PADDING`，状态栏由窗口安全区负责避让；仅展示页允许扩展到全屏。点阵最小字号与固定颜色由回归合同锁定。

## 2026-08-12 展示对比度与点击响应

- `Utils.ensureTextContrast()` 使用 sRGB 相对亮度和 WCAG 对比度公式，要求 LED 正文对其实际背景至少 `4.5:1`。合格的用户颜色保持不变；不合格、无效或彩虹轮换帧会选择黑/白中对比度更高的颜色。黑/白在任意不透明 sRGB 背景中至少有一者达到该门槛。
- `LedPanel` 对其固定 `#01060C` 面板背景执行保护，`DisplayPage` 对当前 `BannerConfig.backgroundColor` 执行保护。因此首页预览、预设、模板、历史、自定义颜色、恢复的旧配置和全屏动画都不能绕过校验。
- 所有显示动画的可见字幕保持完全不透明：纵向进场不再淡入，呼吸、闪烁和翻页改为 `0.94-1.0` 的轻微缩放，弹幕不再使用 `0.9` 透明度。这样审核在任意动画帧看到的字符仍满足对比度规则。
- 首页、模板和历史先完成 `DisplayPage` 路由，再以 `StorageService.saveDisplayConfig()` 在后台把上次配置和历史记录合并为一次串行 `flush()`。颜色确认、最近颜色、计数器、历史删除和清空同样先更新可见状态，再用后台队列持久化；失败时记录日志，历史失败会重新读取真实数据并向用户提示。

## 2026-08-12 HarmonyOS 质量技能边界

- 本次没有产品界面或交互设计变更。新增内容是用户级全局 Codex 技能，不参与应用打包，也不改变运行时、资源、路由、权限或系统栏行为。
- 技能采用渐进披露：`SKILL.md` 只保留来源选择、适用性判断、证据要求和报告合同；UX、性能/审核 FAQ、AppGallery 审核政策分别存入独立参考文件，按任务范围加载。
- 审查输出必须区分官方 `必须`、`强烈推荐`、`推荐`、项目自定门槛与一般建议；未实测的设备/窗口形态必须标记 `unverified`，不能由源码检查、构建成功或其他设备证据外推为通过。
- 华为官方文档是规则与数值来源；“应用市场官方”论坛 FAQ 只补充典型拒审案例、计时起止点、工具和整改路径，不能覆盖冲突的官方文档。

## 2026-08-12 鸿蒙自检增量结构

- 继续采用渐进披露，不扩写三份既有 reference：新增 `release-preflight-cases.md` 专门承载高频判退案例、运行时证据矩阵和发布前分类/资质决策，避免每次 UX 或性能审查加载无关政策材料。
- 同一规则只保留一个权威定义。布局、对比度、底部导航、深色模式和状态栏的 verdict 分别回到既有 UX 标准；FAQ 只能补充审核案例和操作口径。应用审核 13 章继续由既有 review reference 路由。
- 分类与资质按“实际核心功能 -> 实时分类/标签 -> 备案适用性 -> 受监管功能 -> 对应材料”决策，不根据应用名称、希望获得的流量或整份行业目录推导要求。无法确认的类别、法律适用性或材料充分性必须为 `unverified`。
- FAQ 中的 28vp 是案例操作指引，不是跨设备固定几何。审查必须记录实际系统 inset，并覆盖固定控件、键盘、悬浮按钮、滚动末端、弹窗/半模态和沉浸退出恢复。
- 深色模式和状态栏必须通过系统主题切换后的运行时表面取证；资源配置或构建成功不能独立形成 `pass`。

## 2026-08-12 AppGallery 审核驳回整改

- 历史页标题栏按数据状态构建操作：仅当 `records.length > 0` 时显示 `清空` Text，启用态命中区固定为 48x48；空态改为不透明度未降级、不可点击的 48x48 `Blank`。这样空列表不再暴露低对比度伪禁用命令，同时标题几何保持稳定。有记录时仍保留确认弹窗和清空流程。
- 商店上传图标与运行时图标分工明确：`docs/release/appgallery-icon.png` 是 1024x1024、全画布不透明的单张 AppGallery 上传产物；包内 AppScope/EntryAbility 继续使用前景和背景分层资源，让系统负责启动器/最近任务形状。商店 PNG 由同一 AppScope 背景层和前景层逐像素合成，不替换运行时 layered icon。
- 唯一产品名称为 `光迹字幕`。包内 AppScope、EntryAbility、Index、隐私政策和用户协议保持同名；AGC 商品名、简介、隐私资料和软件资质也必须在提交时使用该名称。包内一致性不能证明 AGC 已保存，外部字段在实际写入、预览和核对前保持 `unverified`。
- 性能验收严格采用华为点击完成 `T2-T1 <= 900ms` 口径。源码中的即时状态更新、合同通过和成功构建只能说明实现顺序，不能替代 AppAnalyzer/DevEco Testing 的 T1/T2 运行时测量；当前工具不可用，因此 3117ms 审核项仍待复测。
- AGC 名称/图标上传、简介、隐私资料、软件资质、正式包上传和重新提审属于确认门控的外部操作；本地整改不授权这些写入。phone 已覆盖空态、有记录态、48vp 清空操作、确认弹窗和取消路径；最终确认删除因共享模拟器前台被其他应用抢占而未形成可归因证据，tablet/2in1 仍需补验。
